export const serverPort = 6000;
export const socketPort = 6001;
export const serverHost = 'localhost';
export const socketHost = 'localhost';
export const serverProtocol = 'http';
export const socketProtocol = 'http';

export const socketHostname = `${socketProtocol}://${socketHost}:${socketPort}`;
export const serverHostname = `${serverProtocol}://${serverHost}:${serverPort}`;
